global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'minecraft'
    static_configs:
      # Si Prometheus tourne dans Docker sur la même machine que Minecraft :
      - targets: ['host.docker.internal:19565']